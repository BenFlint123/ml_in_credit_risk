# Logistic, Lasso, and Ridge Model Comparison

This folder contains code that compares **logistic**, **lasso**, and **ridge** models using the **Weight of Evidence (WoE)** datasets from the branch `feature/machine_learning/simple_woe`.
